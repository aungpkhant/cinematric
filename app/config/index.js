const config = {
  site: {
    domain: "localhost",
    URL: "http://localhost:3000", // dont give "/" at the end
  },
  api: {
    ENDPOINT: "http://localhost:8080/api/",
  },
};

export default config;
